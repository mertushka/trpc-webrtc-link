name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Verify Chrome
        run: google-chrome --version
      - run: npm run check
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: browser-e2e-report
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
      - run: mkdir package-artifact
      - run: npm pack --workspace @mertushka/trpc-webrtc-link --pack-destination package-artifact
      - uses: actions/upload-artifact@v7
        with:
          name: npm-package
          path: package-artifact/*.tgz
          if-no-files-found: error

  runtime:
    needs: check
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 20.19.0
          - 22.18.0
          - 24
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - uses: actions/download-artifact@v8
        with:
          name: npm-package
          path: package-artifact
      - run: node scripts/test-packed-package.mjs --runtime-only --tarball package-artifact/*.tgz
